import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Partnerships",
  description: "Join hands with Dhara Foundation. Partner with us for corporate social responsibility (CSR) initiatives and impactful social welfare programs.",
};

export default function PartnershipLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
